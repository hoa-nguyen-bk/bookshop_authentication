

spring:
  application:
    name: authentication_service
#  datasource:
#    hikari:
#      jdbc-url: jdbc:mysql://localhost:3307/bookstore
#      username: root
#      password: admin123
#      maximum-pool-size: 10
#      minimum-idle: 5
#      idle-timeout: 30000
#      connection-timeout: 30000
#      max-lifetime: 60000
  jpa:
      show-sql: true
      hibernate:
        ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3307/bookstore
    username: root
    password: admin123
  mail:
    host: smtp.gmail.com
    port: 587
    username: student1060632fpt@gmail.com
    password: iazwahsoqeatftrx
    properties:
      mail:
        debug: true # bật chế độ debug
        smtp:
          auth: true
          starttls: # là https
            enable: true
          ssl:
            trust: smtp.gmail.com
    protocol: smtp

jwt:
    secret: dcnNZDycP4dGEJvBO40ibxjap207BpXKr8vwlNP2pl0=
server:
  port: 8081

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

